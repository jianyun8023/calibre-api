address: :8080
debug: false
staticDir: "/Users/zhaojianyun/Developer/project/github/jianyun8023/calibre-api/app/calibre-pages/dist"
tmpDir: ".files"
content:
  server: https://lib.pve.icu
search:
  host: http://192.168.2.236:7700
  apikey:
  index: books
metadata:
  doubanurl: http://192.168.2.236:8075

# MCP 服务器配置
mcp:
  enabled: false                        # 是否默认启用传统 MCP 模式（设为 false 启用 HTTP + SSE MCP）
  server_name: "calibre-mcp-server"     # MCP 服务器名称
  version: "1.1.0"                      # MCP 服务器版本
  base_url: "http://localhost:8080"     # API 基础 URL（用于 MCP 工具调用）
  timeout: 30                           # API 请求超时时间（秒）

milvus:
  host: "192.168.2.236"
  port: 19530
  collection_name: "calibre_books"
  vector_dim: 4096

embedding:
  provider: "ollama"  # 或 "siliconflow"
  ollama:
    api_url: "http://localhost:11434/api/embeddings"
    model: "qwen3-embedding:8b"
  siliconflow:
    api_url: "https://api.siliconflow.cn/v1/embeddings"
    model: "Qwen/Qwen3-Embedding-8B"
    api_token: ""  # 建议使用环境变量 SILICONFLOW_API_TOKEN

qdrant:
  url: "http://192.168.2.236:6333"
  collection: "books"
  timeout: 30

cache:
  dir: ".cache/epub"
  max_size_gb: 5
  cleanup_threshold: 0.9
