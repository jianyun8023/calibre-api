---
kind: pipeline
type: docker
name: default

steps:
    - name: build
      image: golang
      commands:
        - go build
        - go test
    - name: docker
      image: plugins/docker
      settings:
        repo: calibre-api
        tags: latest